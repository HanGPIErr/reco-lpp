<UserControl x:Class="RecoTool.Windows.TodoListSessionWarning"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="400">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Border x:Name="WarningBorder" CornerRadius="6" Padding="10,7" Margin="0,0,0,2">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="#E8F4FD"/>
                <Setter Property="BorderBrush" Value="#90CAF9"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>
        </Border.Style>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Icon -->
            <TextBlock Grid.Column="0" Text="&#xE716;" FontFamily="Segoe MDL2 Assets" FontSize="16" 
                       Foreground="#1976D2" VerticalAlignment="Center" Margin="0,0,10,0"/>

            <!-- Content -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <!-- Summary line -->
                <TextBlock x:Name="SummaryText" FontSize="12" Foreground="#1565C0" FontWeight="SemiBold"
                           Text="{Binding SummaryMessage}" TextTrimming="CharacterEllipsis"/>

                <!-- User details (expandable) -->
                <ItemsControl x:Name="SessionsList" ItemsSource="{Binding ActiveSessions}" Margin="0,3,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,1,0,1">
                                <Ellipse Width="6" Height="6" Fill="#4CAF50" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock VerticalAlignment="Center" FontSize="11" Foreground="#37474F">
                                    <Run Text="{Binding UserName, Mode=OneWay}" FontWeight="SemiBold"/>
                                    <Run Text=" — "/>
                                    <Run Text="{Binding StatusText, Mode=OneWay}"/>
                                    <Run Text="{Binding DurationText, Mode=OneWay}" Foreground="#90A4AE" FontSize="10"/>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Join/leave notification -->
                <TextBlock x:Name="NotificationText" FontSize="10" Foreground="#FF8F00" FontStyle="Italic"
                           Text="{Binding NotificationMessage}" Margin="0,2,0,0"
                           Visibility="{Binding HasNotification, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>

            <!-- Dismiss / timestamp -->
            <TextBlock Grid.Column="2" Text="{Binding LastRefreshText}" FontSize="9" Foreground="#90A4AE" 
                       VerticalAlignment="Center" Margin="10,0,0,0"/>
        </Grid>
    </Border>
</UserControl>
