<Window x:Class="RecoTool.Windows.SpiritGeneDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Transaction Details"
        Width="680" Height="580"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="🔎" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <StackPanel>
                <TextBlock x:Name="TitleText" FontSize="15" FontWeight="Bold" Foreground="#1565C0"/>
                <TextBlock x:Name="SubTitleText" FontSize="11" Foreground="#666"/>
            </StackPanel>
        </StackPanel>

        <Separator Grid.Row="1" Margin="0,0,0,8"/>

        <!-- Fields -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="FieldsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="210"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Label}"
                                       FontWeight="SemiBold" Foreground="#424242"
                                       VerticalAlignment="Top" Margin="0,0,12,0"
                                       TextWrapping="Wrap"/>
                            <TextBlock Grid.Column="1" Text="{Binding Value}"
                                       Foreground="#212121" TextWrapping="Wrap"
                                       VerticalAlignment="Top"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                                <Setter Property="TextElement.Foreground" Value="#1565C0"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="CopyButton" Content="📋 Copy All" Click="CopyButton_Click"
                    Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Close" IsCancel="True" Click="CloseButton_Click" Padding="12,6"/>
        </StackPanel>
    </Grid>
</Window>
